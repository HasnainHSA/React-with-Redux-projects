// WHAT IS ACTION

// action = what to do ?


// first action for increment
export const incnum = () => {
    return{
        type: 'INCREMENT',
    }
}

// first action for decrement
export const decnum = () => {
    return{
        type: 'DECREMENT',

    }
}

// first action for rest
export const restnum = () => {
    return{
        type: 'REST',
    }
}